# AIMA — Apple TV Companion (Future)

Status: **Deferred.** Not part of the MVP or Foundation Sprint.

Per `docs/PRODUCT_BIBLE.md` §7 and `docs/TECHNICAL_ARCHITECTURE.md` §2, this is a presentation-only surface (e.g., ambient daily briefing, MFS production board) planned for a future version, after the core product is proven. The shared-SwiftUI approach used by `apps/macos/` and `apps/ios/` is expected to extend here without rearchitecting — `apps/Shared/AIMACore` (Phase 2.1) already targets tvOS in its `Package.swift` platform list, ready for whenever this app is scaffolded.

No scaffolding exists yet. This folder is a structural placeholder.
